// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"step/internal/data/ent/portrait"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PortraitCreate is the builder for creating a Portrait entity.
type PortraitCreate struct {
	config
	mutation *PortraitMutation
	hooks    []Hook
}

// SetUserID sets the "user_id" field.
func (pc *PortraitCreate) SetUserID(s string) *PortraitCreate {
	pc.mutation.SetUserID(s)
	return pc
}

// SetDimension sets the "dimension" field.
func (pc *PortraitCreate) SetDimension(po portrait.Dimension) *PortraitCreate {
	pc.mutation.SetDimension(po)
	return pc
}

// SetValue sets the "value" field.
func (pc *PortraitCreate) SetValue(m map[string]interface{}) *PortraitCreate {
	pc.mutation.SetValue(m)
	return pc
}

// SetID sets the "id" field.
func (pc *PortraitCreate) SetID(u uint64) *PortraitCreate {
	pc.mutation.SetID(u)
	return pc
}

// Mutation returns the PortraitMutation object of the builder.
func (pc *PortraitCreate) Mutation() *PortraitMutation {
	return pc.mutation
}

// Save creates the Portrait in the database.
func (pc *PortraitCreate) Save(ctx context.Context) (*Portrait, error) {
	return withHooks(ctx, pc.sqlSave, pc.mutation, pc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PortraitCreate) SaveX(ctx context.Context) *Portrait {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PortraitCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PortraitCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PortraitCreate) check() error {
	if _, ok := pc.mutation.UserID(); !ok {
		return &ValidationError{Name: "user_id", err: errors.New(`ent: missing required field "Portrait.user_id"`)}
	}
	if _, ok := pc.mutation.Dimension(); !ok {
		return &ValidationError{Name: "dimension", err: errors.New(`ent: missing required field "Portrait.dimension"`)}
	}
	if v, ok := pc.mutation.Dimension(); ok {
		if err := portrait.DimensionValidator(v); err != nil {
			return &ValidationError{Name: "dimension", err: fmt.Errorf(`ent: validator failed for field "Portrait.dimension": %w`, err)}
		}
	}
	if _, ok := pc.mutation.Value(); !ok {
		return &ValidationError{Name: "value", err: errors.New(`ent: missing required field "Portrait.value"`)}
	}
	return nil
}

func (pc *PortraitCreate) sqlSave(ctx context.Context) (*Portrait, error) {
	if err := pc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = uint64(id)
	}
	pc.mutation.id = &_node.ID
	pc.mutation.done = true
	return _node, nil
}

func (pc *PortraitCreate) createSpec() (*Portrait, *sqlgraph.CreateSpec) {
	var (
		_node = &Portrait{config: pc.config}
		_spec = sqlgraph.NewCreateSpec(portrait.Table, sqlgraph.NewFieldSpec(portrait.FieldID, field.TypeUint64))
	)
	if id, ok := pc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := pc.mutation.UserID(); ok {
		_spec.SetField(portrait.FieldUserID, field.TypeString, value)
		_node.UserID = value
	}
	if value, ok := pc.mutation.Dimension(); ok {
		_spec.SetField(portrait.FieldDimension, field.TypeEnum, value)
		_node.Dimension = value
	}
	if value, ok := pc.mutation.Value(); ok {
		_spec.SetField(portrait.FieldValue, field.TypeJSON, value)
		_node.Value = value
	}
	return _node, _spec
}

// PortraitCreateBulk is the builder for creating many Portrait entities in bulk.
type PortraitCreateBulk struct {
	config
	err      error
	builders []*PortraitCreate
}

// Save creates the Portrait entities in the database.
func (pcb *PortraitCreateBulk) Save(ctx context.Context) ([]*Portrait, error) {
	if pcb.err != nil {
		return nil, pcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Portrait, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PortraitMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = uint64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PortraitCreateBulk) SaveX(ctx context.Context) []*Portrait {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PortraitCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PortraitCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
